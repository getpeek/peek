[package]
name = "peek-acp"
version = "0.1.0"
edition = "2024"

[dependencies]
agent-client-protocol = "1.3.0"
tokio = { version = "1", features = ["sync", "rt"] }
serde_json = "1"
async-trait = "0.1"
anyhow = "1"

[lints]
workspace = true
